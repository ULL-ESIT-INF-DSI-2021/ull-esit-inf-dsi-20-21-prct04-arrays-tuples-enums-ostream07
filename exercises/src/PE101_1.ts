/**
 * Cree una función encodeMessage que reciba como parámetro una cadena 
 * de caracteres que debe ser cifrada mediante un algoritmo de sustitución 
 * en el que cada letra del alfabeto de entrada (abcdefghijklmnopqrstuvwxyz) 
 * se sustituye por la letra correspondiente del mismo alfabeto pero ordenado 
 * de manera inversa (zyxwvutsrqponmlkjihgfedcba). De este modo, por ejemplo, 
 * la letra 'a' se sustituiría por la letra 'z', y viceversa. El resultado de 
 * la invocación a la función debe ser un array de cadenas de caracteres, en 
 * el que cada cadena de dicho array tiene longitud cinco y se corresponde con 
 * cinco caracteres del mensaje de entrada ya codificados. Al mismo tiempo, la 
 * función debe devolver undefined en el caso de que hayan caracteres no permitidos 
 * en la cadena de entrada. Implemente también la función decodeMessage que lleve 
 * a cabo la operación inversa de decodificación. En este caso, el parámetro de 
 * entrada de la función deberá ser un array de cadenas de caracteres de longitud 
 * cinco y deberá devolver una única cadena de caracteres con el mensaje 
 * decodificado sin espacios. En caso de que alguna de las cadenas de caracteres 
 * el array de entrada no tenga longitud cinco o incluya caracteres no válidos, 
 * la función deberá devolver undefined.
 * 
 */
/*
let text: string = `abcde`;
let inverseAlphabet: string = `zyxwvutsrqponmlkjihgfedcba`; 
encodeMessage(text);

function encodeMessage(text) {

}
*/